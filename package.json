{
  "name": "finder",
  "displayName": "Finder",
  "description": "Fast file and text search popup inspired by Neovim Telescope and JetBrains Finder",
  "version": "0.1.0",
  "publisher": "piotrmacai",
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "search",
    "finder",
    "telescope",
    "grep",
    "fuzzy"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/piotrmacai/finder"
  },
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "finder.open",
        "title": "Finder: Open Search"
      }
    ],
    "keybindings": [
      {
        "command": "finder.open",
        "key": "shift+alt+f",
        "mac": "shift+alt+f",
        "when": "editorTextFocus || !inputFocus"
      }
    ],
    "configuration": {
      "title": "Finder",
      "properties": {
        "finder.ripgrepPath": {
          "type": "string",
          "default": "",
          "description": "Path to ripgrep binary. Leave empty to use bundled version."
        },
        "finder.maxResults": {
          "type": "number",
          "default": 200,
          "description": "Maximum number of search results to display."
        },
        "finder.defaultScope": {
          "type": "string",
          "enum": [
            "project",
            "openFiles",
            "files"
          ],
          "default": "project",
          "description": "Default search scope when Finder opens."
        },
        "finder.keybindings.navigateDown": {
          "type": "string",
          "default": "ArrowDown",
          "description": "Key to navigate down in results (e.g. 'ArrowDown', 'j')."
        },
        "finder.keybindings.navigateUp": {
          "type": "string",
          "default": "ArrowUp",
          "description": "Key to navigate up in results (e.g. 'ArrowUp', 'k')."
        },
        "finder.keybindings.open": {
          "type": "string",
          "default": "Enter",
          "description": "Key to open the selected result."
        },
        "finder.keybindings.close": {
          "type": "string",
          "default": "Escape",
          "description": "Key to close Finder."
        },
        "finder.keybindings.toggleRegex": {
          "type": "string",
          "default": "shift+alt+r",
          "description": "Key combination to toggle regex mode (e.g. 'shift+alt+r')."
        },
        "finder.keybindings.togglePreview": {
          "type": "string",
          "default": "shift+alt+p",
          "description": "Key combination to toggle preview panel (e.g. 'shift+alt+p')."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@vscode/ripgrep": "^1.17.0"
  }
}
